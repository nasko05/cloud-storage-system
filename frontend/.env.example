# Frontend configuration (only needed for local dev against a separate backend).
#
# In production the FastAPI server serves the built frontend, so requests are
# same-origin and this can be left empty. For `npm run dev` against a backend on
# another port, point it at that backend:
VITE_API_ENDPOINT=http://localhost:8000
